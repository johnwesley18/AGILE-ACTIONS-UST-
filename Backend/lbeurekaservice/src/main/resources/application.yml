spring:
  application:
    name: LBEUREKASERVICE
   
server:
  port: 8761


eureka:

 client:

  registerWithEureka: false

  fetchRegistry: false
 instance:
   hostname: localhost
 server:
  
  wait-time-in-ms-when-sync-empty: 0



  # Optional: Disable self-preservation if needed


